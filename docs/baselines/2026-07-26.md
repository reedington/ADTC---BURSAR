# Verified baseline — 2026-07-26

This snapshot records the repository state immediately before the next-build roadmap began.
Commands ran from the repository root with the checked-in Python environment.

## Automated checks

- `.venv/bin/pytest -q`: passed (one hardware-bound tokenizer test skipped).
- `.venv/bin/python -m bursa_eval.goldcheck data/gold`: 14/14 cases valid, 14/14
  scenario families covered, languages `en` and `pcm`, 3/14 authored abstentions.
- `.venv/bin/python -m bursa_eval.harness.scorecard --backend fake --out runs
  --label baseline-2026-07-26`: safety gates passed; zero incorrect automatic posts,
  duplicate-blocked rate 1.0, action accuracy 1.0, top-1 student accuracy 1.0.

The fake-backend scorecard validates harness wiring and safety mechanics only. It is not evidence
of real-model quality or performance.

## Gate 1 checker

`.venv/bin/python -m bursa_eval.submission_check --gate1` correctly failed on five outstanding
external/finalization requirements:

1. registered ADTF team ID;
2. submitter name;
3. submitter email;
4. target-hardware `submission.json`;
5. two-minute demonstration video.

No benchmark or competition-readiness claim is inferred from this baseline.
