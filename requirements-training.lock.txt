# Direct training dependencies. The completed run also records `pip freeze`
# so every transitive CUDA/PyTorch wheel is captured with the artifacts.
unsloth[cu128-torch280]==2026.7.5
transformers==5.14.1
trl==1.9.1
datasets==5.0.0
accelerate==1.14.0
